var Layouts = (function(app, versionManager, auditManager, AdminException){
	server.api.registerRoute(app, "admin/layout-schemes")
		.get(function(req, res){
			var session = server.db.getSession();
			var statement = server.db.generatePreparedStatement({
				
			});
		});
});